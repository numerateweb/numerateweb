Prefix: : <http://example.org/>

Class: Rectangle
Constraint:
area = @a * @b

Class: Rectangles
Constraint:
areaSum = sum(@@rectangles, $r -> @area($r)),
sumIs9  = @areaSum = 9.0

Class: NAry
Constraint:
add      = @a + @b + @c,
multiply = @a * @b * @c,
divide   = @a / @b / @c,
subtract = @a - @b - @c,
power    = @a ^ @b ^ @c
